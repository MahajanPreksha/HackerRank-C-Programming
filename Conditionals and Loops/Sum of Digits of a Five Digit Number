#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	int n, rem, sum = 0, i;
    scanf("%d", &n);
    for (i = 1; i<=5;i++){
        rem = n%10;
        sum += rem;
        n = n/10;
    }
    printf("%d", sum);
    return 0;
}
